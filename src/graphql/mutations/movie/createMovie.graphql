mutation CreateMovie(
  $title: String!
  $description: String
  $genres: [String!]!
) {
  createMovie(
    options: { title: $title, description: $description, genres: $genres }
  ) {
    movie {
      id
      title
      description
    }
    errors {
      ...ErrorFragment
    }
  }
}
