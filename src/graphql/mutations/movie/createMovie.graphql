mutation CreateMovie(
  $title: String!
  $description: String
  $photo: String
  $genres: [String!]!
) {
  createMovie(
    options: {
      title: $title
      description: $description
      photo: $photo
      genres: $genres
    }
  ) {
    message
    errors {
      ...ErrorFragment
    }
  }
}
