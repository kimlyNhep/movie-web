mutation updateMovieState($mid: String!, $state: String!) {
  updateMovieState(mid: $mid, options: $state) {
    errors {
      field
      message
    }
    user {
      id
      username
    }
    movie {
      id
      title
    }
  }
}
